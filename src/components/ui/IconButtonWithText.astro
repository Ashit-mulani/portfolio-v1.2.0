---
const { class: className = "" } = Astro.props;
---

<button
  class={`${className} smooth cursor-pointer px-4 py-2 text-sm flex items-center gap-2 justify-center text-neutral-500`}
>
  <slot />
</button>
